interface:
  display_name: "Agent Bridge"
  short_description: "Run and debug the multi-channel agent bridge"
  default_prompt: "Use $agent-bridge to set up, operate, or troubleshoot this channel-to-agent bridge."
